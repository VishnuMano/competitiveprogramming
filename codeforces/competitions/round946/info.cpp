#include <bits/stdc++.h>

using namespace std;

int main()
{
    // Dynamic Array
    vector<int> demo_vect = {1, 2, 3, 4};

    // Linked List
    list<int> demo_list = {1, 2, 3, 4};

    // Stack
    stack<int> demo_stack = {1, 2, 3, 4};

    // Queue
    queue<int> demo_queue = {1, 2, 3, 4};

    // HashMap
    unordered_map<int, string> demo_map;
    map[1] = "one";
    map[2] = "two";
    map[3] = "three";

    // Tree
    
}